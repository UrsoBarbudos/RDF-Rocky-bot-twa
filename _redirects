# Cloudflare Pages redirects

# Root redirect to profile (теперь используем index.html в папке profile)
/  /profile/  200

# Fallback для неизвестных путей
/admin/*  /profile/  200
/api/*  /profile/  200